<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maipatgeorge.tequila.ictwordguessinggames">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OpeningMenu" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".GuestStartGame"
            android:label="@string/title_activity_guest_start_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FBuserStartGame"
            android:label="@string/title_activity_fbuser_start_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GuestDB" />
        <activity android:name=".GuestWL" />
        <activity android:name=".GuestSEC" />
        <activity android:name=".FBuserWL" />
        <activity android:name=".FBuserSEC" />
        <activity android:name=".FBuserDB"></activity>
    </application>

</manifest>